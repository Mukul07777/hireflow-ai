# Screenshots

The README embeds three images from this folder. Save them with these **exact
filenames** (PNG, ideally ~1400px wide, taken on the live site):

| Filename | What to capture |
|---|---|
| `home.png` | The home screen showing all seven AI systems + the separated Team card |
| `company-brain.png` | The Company Brain screen — stat strip, next-best-actions, a merged identity |
| `closed-loop.png` | SalesFlow → Outreach tab, showing the ⚠️ account-check warning on a prospect card |

Tip: capture at a browser width of ~1400px with the page zoomed to 100% so text stays crisp.
